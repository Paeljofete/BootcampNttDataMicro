server:
  port: 8086
spring:
  config:
    impor: configserver:http://localhost:8888
  application: 
    name: product-service
  cloud:
    config:
      enabled: true
      ui: http://localhost:8888
